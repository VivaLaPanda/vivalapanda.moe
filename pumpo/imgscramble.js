/* const MAX_IMAGES = 104;

const images = new Array(MAX_IMAGES);
for (let index = 0; index < MAX_IMAGES; index++) {
images[index] = `https://vivalapanda.moe/pumpo/banner${index}.png`;
}

for (const link of document.getElementsByClassName("link")) {
const index = Math.floor(Math.random() * images.length);
link.src = images[index];
images.splice(index, 1);
} */